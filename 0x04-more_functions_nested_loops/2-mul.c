#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two intigers
 * @a: - The first intiger
 * @b: - The second intiger
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
